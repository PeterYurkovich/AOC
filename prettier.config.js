/** @type {import("prettier").Config} */
const config = {
  semi: true,
  arrowParens: 'avoid',
  trailingComma: 'all',
  singleQuote: false,
  endOfLine: 'lf',
  bracketSpacing: true,
  printWidth: 80,
  useTabs: false,
  quoteProps: 'as-needed',
  tabWidth: 4,
}

export default config
